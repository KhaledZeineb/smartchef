<form method="post" action="{{ path('app_user_ingredient_delete', {'id': user_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_ingredient.id) }}">
    <button class="btn">Delete</button>
</form>
